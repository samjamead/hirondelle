export const metadata = {
  title: 'Aonach Beag in glorious sunshine',
  date: '2024-02-24',
  category: 'Mountains',
  distance: 14.5,
  elevation: 831,
  duration: 267,
  mountains: ['Aonach Mòr', 'Aonach Beag'],
};

import PostHero from '@/_components/post-hero';
import { ArrowRightCircle, ArrowUpCircle, Clock12 } from 'lucide-react';

export const durationString = `${
  (metadata.duration - (metadata.duration % 60)) / 60
}hrs ${metadata.duration % 60}mins`;

<PostHero metadata={metadata} />

<div className='mb-8 flex items-center gap-8'>
  <p className='flex items-center gap-2'>
    <ArrowRightCircle size={24} strokeWidth={1.5} />{' '}
    <span>{metadata.distance}km</span>
  </p>
  <p className='flex items-center gap-2'>
    <ArrowUpCircle size={24} strokeWidth={1.5} />{' '}
    <span>{metadata.distance}m</span>
  </p>
  <p className='flex items-center gap-2'>
    <Clock12 size={24} strokeWidth={1.5} /> <span>{durationString}</span>
  </p>
</div>

<img
  src='/img/aonachbeagfeb24/IMG_5648.jpeg'
  alt='The Ben'
  className='max-w-full rounded-md'
/>
